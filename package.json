{
  "name": "otjobber",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "start": "npm run start -w frontend",
    "lint": "npm run lint -w frontend",
    "backend:deploy:dev": "cd backend && npx cdk deploy --all --context environment=dev",
    "backend:deploy:staging": "cd backend && npx cdk deploy --all --context environment=staging",
    "backend:deploy:prod": "cd backend && npx cdk deploy --all --context environment=prod",
    "backend:test": "npm run test -w backend"
  }
}
